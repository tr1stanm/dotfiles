hl.config({
    input = {
        touchpad = {
            tap_to_click = false, -- Disable tap to click
            -- Optional: Disable touchpad while typing to prevent accidental clicks
            disable_while_typing = true,
            natural_scroll = true,
            clickfinger_behavior = true
        },
    },
})
